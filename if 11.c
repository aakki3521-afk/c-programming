#include <stdio.h>
int main() {
 int a;
 printf("enter the num=");
 scanf("%d",&a);
 if (a>=9 && a<=9)
 {
    printf("its a single integer");
 }
 else{
    printf("not a single integer");
 }
    
    return 0;
}