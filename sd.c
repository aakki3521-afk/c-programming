#include <stdio.h>
int main() {
    int a,b,c;
        printf("enter the values");
       scanf("%d %d",&a,&b);
    printf("enter the values");
    c=a/b;
    printf("c is the valur of%d",c);
    
    return 0;
}